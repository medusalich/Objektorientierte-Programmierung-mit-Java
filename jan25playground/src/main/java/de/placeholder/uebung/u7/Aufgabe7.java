package de.placeholder.uebung.u7;

public class Aufgabe7 {

    /*
    Entwickle eine ProduktFaker-Klasse.
    In getrennte Arrays kommen:
    10x Name
    10x Beschreibung
    10x Preis
    beim Aufruf der fake-Methode wird die Anzahl gewünschter Produkte vorgegeben.
    Die Methode erzeugt ein Array in passender Größe und befüllt es mit Produkten.
    Der Inhalt der Produkte wird nach Zufall aus den Array (Titel, Beschreibung, Preis) zusammengestellt.
    Anzahl kann eine einfache Zufallszahl sein.

    ProduktFaker.fake(10) liefert ein Produkt[] mit 10 Produkten zurück.

    //Hilfe
    Random rand = new Random()
    int zahl = rand.nextInt(10); //0...9
    name[zahl]
    */
}
